"""partest-load — нагрузочный харнесс семейства partest.

Нагрузка задаётся временем, а не количеством запросов: интересует не «сколько отправили»,
а «где система перестала держать». Пакет пишет свои результаты в каталог прогонов и умеет
собрать по ним HTML-дашборд.

Артефакта `load.profile` для внешнего потребителя пакет **пока не пишет**: модуля
`artifacts.py` нет, и формат ещё не согласован с владельцем контракта артефактов.
Предыдущая редакция этой строки обещала его в настоящем времени — то есть
`help(partest_load)` сообщал неправду тому, кто пришёл за этим артефактом.
"""

# Единственный источник правды о версии — этот литерал; `setup.py` читает его отсюда
# регулярным выражением. Так же устроены `partest` и `partest-gen`, и порядок в семействе
# один: номер объявляется в пакете, сборка его забирает. Обратное объявление (литерал в
# метаданных дистрибутива, а здесь чтение через `importlib.metadata`) давало верный номер
# только у установленного пакета: из рабочего дерева `__version__` был `0.0.0.dev0`, а при
# старой установке рядом — номером той установки, а не этого кода.
__version__ = "0.2.0"

from .core.runner import run_load_test  # noqa: E402

__all__ = ["__version__", "run_load_test"]
