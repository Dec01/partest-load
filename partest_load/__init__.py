"""partest-load — нагрузочный харнесс семейства partest.

Нагрузка задаётся временем, а не количеством запросов: интересует не «сколько отправили»,
а «где система перестала держать». Пакет пишет свои результаты в каталог прогонов и умеет
собрать по ним HTML-дашборд.

Артефакта `load.profile` для карты «Веха» пакет **пока не пишет**: модуля `artifacts.py`
нет, и формат ещё не согласован с владельцем контракта артефактов. Предыдущая редакция
этой строки обещала его в настоящем времени — то есть `help(partest_load)` сообщал
неправду тому, кто пришёл за этим артефактом.
"""

import importlib.metadata as _metadata

# Версия объявлена в одном месте — в `pyproject.toml`, и читается из метаданных пакета.
# Второй литерал здесь расходился бы с первым тихо, а обнаруживалось бы это у потребителя.
try:  # pragma: no cover - зависит от способа установки
    __version__ = _metadata.version("partest-load")
except _metadata.PackageNotFoundError:  # работа из рабочего дерева
    __version__ = "0.0.0.dev0"

from .core.runner import run_load_test  # noqa: E402

__all__ = ["__version__", "run_load_test"]
