"""HTML-дашборд прогона: данные графиков и сборка страницы.

Подпакет живёт за extra `report`: `numpy`, `pandas`, `plotly` и `Jinja2` весят больше,
чем всё остальное вместе, а тому, кто снимает только артефакт, они не нужны.
Поэтому здесь ничего не импортируется на уровне пакета — иначе `import partest_load`
требовал бы отчётных зависимостей у всех.

Нужное берётся из модулей напрямую::

    from partest_load.reporting.generator import generate_dashboard
"""

__all__: list[str] = []
