---
title: Статус partest-load
status: current
verified: 2026-09-23
sources: [partest_load/__init__.py, CHANGELOG.md, setup.py]
audience: agent
allow_version_literals: true
---

# Статус

**Единственный источник** версии, состояния и того, что дальше. В других страницах этих
сведений нет — там пишут «см. [[status]]».

## Где сейчас

| | |
|---|---|
| Версия | 0.1.0, не выпущена |
| PyPI | нет; дистрибутив к первому выпуску подготовлен и проверен сборкой |
| Python | ≥ 3.10 |
| Лицензия | MIT |
| Под git | да, с 2026-09-07; удалённого репозитория ещё нет |
| Перенос | из `github.com/Dec01/load`; новый адрес `github.com/Dec01/partest-load` не создан |

## Что есть в коде

Перенос прототипа со снятием продуктового домена сделан. В дереве:

| Где | Что |
|---|---|
| `configs/` | схемы профиля (`linear`, `ramp`, `stress`, `batch`) и описания эндпоинта |
| `core/` | клиент, метрики, пейсинг, воркер, раннер |
| `engines/` | четыре движка; у каждого `plan()` — расписание фаз, считаемое без стенда |
| `operation.py` | операция прогона «МЕТОД /путь» и её ключ в имени файла |
| `storage/` | схема прогона (формат `2.0`) и хранилище, ключуемое операцией |
| `reporting/` | данные графиков, сборка дашборда, три шаблона Jinja2 |
| `cli.py` | точка входа `partest-load`, на которую ссылается `console_scripts` |

Тесты есть, сети и стенда не требуют: метрики, профили, расписание каждого движка, критерий
обрыва stress, хранилище, сборка дашборда.

## Чего в коде нет

Обещать это потребителю нельзя:

- **выгрузки артефакта `load.profile`** — модуля `artifacts.py` нет. Результат прогона читает
  только дашборд, внешний потребитель артефактов пока не получает ничего;
- **переиспользования `partest`**: `TokenManager`, `http.Config`, `openapi`, `path_match`,
  `redact` не подключены. Зависимость в `setup.py` объявлена, но кодом не используется;
- **редактирования секретов через `partest.redact`**: снимок конфигурации прогона оставляет
  имена заголовков и метки вариантов тела, а значения не берёт (`EndpointConfig.snapshot()`),
  но это своя простая мера, а не общее для семейства редактирование;
- **маркировки создаваемых данных** (`partest.data_marker`);
- остановки прогона по `stop_on_error_rate` и `stop_on_p95_latency_ms` в профилях linear и
  ramp: поля разбираются и проверяются, но решение об обрыве принимает только stress.

## Что дальше

1. `artifacts.py` — выгрузка `load.profile`. Формат чужой, см. `partest_atlas.artifacts`;
   изменение формата согласуется с владельцем контракта.
2. Переиспользование `partest`: сначала `redact` (секреты в снимке конфигурации), затем
   `TokenManager` и `http.Config`, затем список эндпоинтов из спецификации.
3. Остановка прогона по порогам профиля для linear и ramp.

Работы перечислены в `CHANGELOG.md` и здесь не дублируются.

## Что мешает выпуску

Ничего, что решалось бы в этом репозитории. Упаковка описана `setup.py` и `setup.cfg`, как
у `partest` и `partest-gen`; `pyproject.toml` в репозитории нет. Собирается обеими командами
семейства — `python -m build` и `python setup.py sdist bdist_wheel`, — `twine check` зелёный,
колесо ставится в чистое окружение, `partest-load --help` и `--dry-run` работают из
установленного пакета. В колесе — пакет с шаблонами отчёта и `py.typed`, в дистрибутиве —
`LICENSE`, `CHANGELOG.md` и `docs/PYPI.md`; тестов, вики и `README.md` в нём нет. Номер версии
объявлен литералом в `partest_load/__init__.py`, `setup.py` читает его оттуда.

Осталось внешнее и не наше: удалённого репозитория `github.com/Dec01/partest-load` ещё нет,
а на него ссылаются `project_urls`. Выкладывает владелец: выпуск на PyPI — его решение,
не агента.

## Открытые вопросы

- Зависимость объявлена как `partest>=1.8`, хотя семейство уже на 2.0.0, а кодом `partest`
  пока не используется вовсе. Граница уточняется вместе с первым переиспользованием.
- Старый репозиторий `github.com/Dec01/load` ещё существует. Решение о переносе истории
  (или об отказе от неё) не принято и должно быть принято до создания нового удалённого
  репозитория: после первого push выбор становится дороже.
- Значения нагрузки в примерах: нужен очевидно выдуманный стенд, чтобы числа из примера
  никогда не были процитированы как характеристика библиотеки.
